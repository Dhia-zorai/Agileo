This repository contains a production-grade Agileo dashboard prototype with a FastAPI backend and a React frontend. Run scripts below to start the full stack with a single command.

One-command startup: `npm run dev` from the repository root.

Backend runs on http://localhost:8000/api and frontend on http://localhost:5173.
